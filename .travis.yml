sudo: false
language: python
cache: pip

matrix:
  include:
    - python: "3.6"
    - python: "3.5"
    - python: "3.4"
    - python: "2.7"

install:
  - pip install tox-travis
  - pip install -U pip setuptools

script: tox -v
